#pragma once
#include <vector>

// LeetCode 3702. Longest Subsequence With Non-Zero Bitwise XOR
// Return the length of the longest subsequence of nums whose bitwise XOR
// is non-zero. If every element is 0, no such subsequence exists (return 0).
// Time: O(n), Space: O(1)
int longestSubsequence(const std::vector<int>& nums);
